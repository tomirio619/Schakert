language: java
# Build command
script: "mvn -DskipTests=false jfx:jar"

sudo: false

matrix:
    include:
        - jdk: oraclejdk8
        addons:
          apt:
            packages:
              - oracle-java8-installer
